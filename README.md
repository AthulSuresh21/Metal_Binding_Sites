# Metal_Binding_Sites
Python based pipeline to scrabe .pdb/mmcif data from RSC pdb repository to scrape data for metal binding proteins, and the analysis of metal binding sites.

0) Install the dependencies:
   conda env create -f biometal.yml
   conda activate biometal
   pip install rcsb-api
   
